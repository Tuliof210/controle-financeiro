import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography" />

export const sizes = [
  "text-2xs", "text-xs", "text-sm", "text-base", "text-md", "text-lg",
  "text-xl", "text-2xl", "text-3xl",
];

export const weights = ["weight-regular", "weight-medium", "weight-bold"];

# Typography

One display face, one workhorse face. `--font-display` (Press Start 2P) is
for short titles/eyebrows only — never body copy, never long numbers.
Everything else, including every number, uses `--font-mono` (JetBrains Mono).

## Display — short text only

<div style={{ fontFamily: "var(--font-display)", fontSize: "var(--text-2xl)", letterSpacing: "var(--tracking-display)", lineHeight: "var(--leading-tight)", background: "var(--color-surface)", color: "var(--color-text)", padding: "var(--space-4)" }}>
  {"CAIXA SAUDÁVEL"}
</div>

## Mono — everything else

<p style={{ fontFamily: "var(--font-mono)", fontSize: "var(--text-md)", lineHeight: "var(--leading-normal)", maxWidth: "42ch", background: "var(--color-surface)", color: "var(--color-text)", padding: "var(--space-3)" }}>
  {"Compare o esperado com o que aconteceu no mês — este é o corpo de texto padrão da UI, sempre em "}
  <code>--font-mono</code>
  {"."}
</p>

## Size scale

<div style={{ display: "flex", flexDirection: "column", gap: "var(--space-2)", fontFamily: "var(--font-mono)", background: "var(--color-surface)", padding: "var(--space-4)" }}>
  {sizes.map((name) => (
    <div key={name} style={{ display: "flex", alignItems: "baseline", gap: "var(--space-4)" }}>
      <code style={{ width: "8ch", fontSize: "var(--text-xs)", color: "var(--color-text-muted)", fontFamily: "var(--font-mono)" }}>
        --{name}
      </code>
      <span style={{ fontSize: `var(--${name})`, fontFamily: "var(--font-mono)", color: "var(--color-text)" }}>Aa 123</span>
    </div>
  ))}
</div>

## Weights

<div style={{ display: "flex", gap: "var(--space-6)", fontFamily: "var(--font-mono)", fontSize: "var(--text-lg)", background: "var(--color-surface)", color: "var(--color-text)", padding: "var(--space-4)" }}>
  {weights.map((name) => (
    <span key={name} style={{ fontWeight: `var(--${name})`, fontFamily: "var(--font-mono)", color: "var(--color-text)" }}>
      Aa — --{name}
    </span>
  ))}
</div>

## Money — mono + tabular-nums (mandatory)

<div style={{ fontFamily: "var(--font-mono)", fontVariantNumeric: "tabular-nums", fontSize: "var(--text-lg)", background: "var(--color-surface)", color: "var(--color-text)", padding: "var(--space-4)" }}>
  {"R$ 1.234,56 · R$ 0,00 · −R$ 88,90"}
</div>
