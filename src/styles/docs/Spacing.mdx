import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Spacing" />

export const steps = [
  "space-1", "space-2", "space-3", "space-4", "space-5", "space-6",
  "space-8", "space-10", "space-12", "space-16", "space-20", "space-24",
];

# Spacing

A closed 4px grid — no ad-hoc pixel values. Every gap, padding, and margin
in the app comes from one of these steps.

<div style={{ display: "flex", flexDirection: "column", gap: "var(--space-2)", fontFamily: "var(--font-mono)" }}>
  {steps.map((name) => (
    <div key={name} style={{ display: "flex", alignItems: "center", gap: "var(--space-4)" }}>
      <code style={{ width: "10ch", fontSize: "var(--text-xs)", color: "var(--color-text-muted)" }}>
        --{name}
      </code>
      <div style={{ height: "16px", width: `var(--${name})`, background: "var(--color-brand)" }} />
    </div>
  ))}
</div>
