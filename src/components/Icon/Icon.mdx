import { Meta } from "@storybook/addon-docs/blocks";
import { Icon } from "./index.tsx";
import { MV_ICONS } from "./icons.ts";

<Meta title="Components/Icon" />

# Icon

Single-stroke glyphs on a 24×24 grid. Stroke 1.75, round caps and joins,
`currentColor`. Pass `title` to promote the SVG to `role="img"`; without it
the mark is presentation.

export const names = Object.keys(MV_ICONS) as Array<keyof typeof MV_ICONS>;

<div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(96px, 1fr))", gap: "var(--space-4)", fontFamily: "var(--font-sans)", fontSize: "var(--text-xs)", color: "var(--color-text)" }}>
  {names.map((name) => (
    <div key={name} style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "var(--space-2)", padding: "var(--space-3)", border: "var(--border-1) solid var(--color-border)", borderRadius: "var(--radius-md)", background: "var(--color-surface)" }}>
      <Icon name={name} size={24} />
      <code style={{ fontFamily: "var(--font-mono)", fontSize: "var(--text-2xs)" }}>{name}</code>
    </div>
  ))}
</div>
