// Nothing to compute — the folder still gets the three-file split so every
// component in the repo is found the same way (SettingsScreen/hook.ts is the
// existing precedent for a constant-only hook).
export function useBrandMark() {
  return { label: "Monevo" };
}
