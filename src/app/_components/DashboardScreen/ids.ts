// The month table's id and ShowAllToggle's aria-controls have to be the same
// string, and they live in sibling components — so the string lives here rather
// than twice. Same reason `src/components/AppShell/ids.ts` exists.
export const CEILING_MONTHS_ID = "ceiling-months";
