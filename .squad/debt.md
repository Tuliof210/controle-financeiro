# Debt — <project>

A place in the code that is knowingly wrong and that nobody is fixing now. Two
sources, no others: a review finding the owner declined, and a shortcut the
implementation took on purpose. Both are decisions someone already made — this file
only stops them from being made again, at full price, every round.

Not learnings. A learning is a rule for code that does not exist yet and points at no
line. Debt points at a line — that is the whole test: **can you name the file, it is
debt; can you not, it is a learning.** Not a task either: what is being fixed is a
task, debt is what was declined. Not a `window:`: a window already has the task that
closes it and `ps-check.sh` blocks the merge until it lands. Nothing here has a
closer, which is exactly why each entry has to say what would earn it one.

No cap — debt grows honestly and capping it would only make the file lie. It is kept
short another way: an entry whose path no longer exists goes with the code, and one
with no `until` is a wish rather than a debt. NOTHING CHECKS EITHER — this file used
to claim `sh .claude/ps-check.sh` flagged both, and that script has only
`warm|publish|sweep` (it exits 1 with no mode). Nine dead `e2e/*` entries survived
here for exactly that reason. It is a convention, kept by whoever edits the file.
An entry leaves by being fixed, or by the code being deleted — never by being
reworded.

Format: `- [<path>:<line-or-symbol>] <what is wrong>, until <what earns it a fix> (YYYY-MM-DD)`

---

- [prisma/schema.prisma:OfxImport] nothing links a movement back to the import that wrote it and nothing clears a `fileHash`, so deleting the imported rows on /movimentacoes leaves that statement permanently unimportable — the owner declined an undo when the story was scoped, until wanting to re-import a statement after correcting it (2026-07-28)
- [src/components/EntryRow/index.tsx:37] the metadata line shows `Owner · Interval(s)` where the design and task 03 both specify a duration too (`24 meses`) — dropped as the line is already tight at 375px and the duration is derivable from the intervals beside it, until the line gains room (2026-07-29)
- [src/components/EntryScreen/components/Sections/style.module.scss:31] the two-column container query only guarantees a post-split column stays above RowGrid's one-line floor, not that it stays above the single-column width it replaces — measured on /previsoes, a row is 683px wide at 768px (single column) and 640px at 1440px (two columns), narrower after growing — until the query also compares against the pre-split width, not just the floor (2026-07-29)
- [src/components/RowGrid/style.module.scss:63, _gutter.scss:7] two separate `[data-cell="who"]` rule blocks (one setting `margin-inline-start: 0` in `_gutter.scss`, one setting `grid-area: who` in `style.module.scss`) could be one — split across files by the 100-line-cap fix in PR #83, cosmetic only, until someone merges them without pushing either file back over its own cap (2026-07-29, found in PR #83 review)
- [src/app/previsoes/_components/ForecastsScreen/components/ForecastForm/components/IntervalList/components/IntervalCard/style.module.scss:26] `.eyebrow` and `.lock` each repeat the same five-declaration micro-label block (mono, `--tracking-wide`, `--color-text-muted`, uppercase, small size) that `DashboardScreen/components/Headline`'s `.caption` already holds, and `.eyebrow` sets `--text-xs` where every other eyebrow in the repo uses `--text-2xs` — until one local mixin holds the five, which also gives back the room the 100-line cap took when `_checkbox.scss` and `_summary.scss` split off (2026-07-29, found in PR #89 review)
- [src/app/previsoes/_components/ForecastsScreen/components/ForecastForm/components/IntervalList/components/IntervalCard/style.module.scss:5, src/app/previsoes/_components/ForecastsScreen/components/ForecastForm/components/IntervalList/components/IntervalCard/_summary.scss:5] both comments name the surface-tier direction backwards, and in opposite directions about the same token pair — the card is `--color-surface`, one tier BELOW the modal's `--color-surface-raised`, and the strip is one tier ABOVE the card — what renders is correct in both themes, only the prose misleads, until someone next edits either file (2026-07-29, found in PR #89 review)
- [src/app/api/dashboard/pace.helper.ts:savingPace] the monthly ceilings decay geometrically (each month is given the SELECTED cap — 25, 50 or 75% — of what the earlier ones left it, so a lower cap flattens the decay and lengthens the tail rather than removing it), so a long period carries a tail of near-zero figures that drags their mean down — the consequence is that recording a far-future forecast LOWERS the savings capacity, which reads backwards to someone who just recorded more income. A property of the plain average the owner chose over a trimmed or weighted one, named in PR #90's Known gaps rather than hidden, until the figure reads as too pessimistic against real data and the extra explanation a weighted mean needs is worth paying for (2026-07-30)
- [src/app/api/dashboard/ceiling.helper.ts:average] second site of the plain-mean property already recorded for `pace.helper.ts:savingPace`, and promoted here from a background figure to a headline AND to the denominator of the "N× a média" chip — measured in review, stretching the payload from 5 to 12 months moved the chip from `2,7×` to `6,2×` with this month's figure unchanged, so recording a far-future forecast makes this month look more like a spike; the story asked for "a média bruta", until that entry's trigger fires and both sites move together (2026-07-30, found in PR #91 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/_badges.scss:.limit, .now] the same five-declaration micro-label block already recorded at `IntervalCard/style.module.scss:26` now has three more independent copies — here, `CeilingCard/style.module.scss:.footer, .chip` and `MeterRow/style.module.scss:.now`; `.chip` also dropped `font-variant-numeric: tabular-nums` from the `GoalRow` `.target` exemplar it was told to imitate, so a chip showing a number lost its tabular figures, until one mixin holds the five and the copies include it (2026-07-30, found in PR #91 review; `.legend` and the deleted Hero card's `.chip` re-pointed in PR #93; PR #94 added three more — `CeilingCard/_split.scss:.rateLabel` verbatim, `StatCard/style.module.scss:.label` completed from three of the five, and `ChartLegend/style.module.scss:.legend` at four of five, its colour `inherit` because it rides the ink band — roughly eight sites now; PR #95 added a ninth, `SavingsSection/style.module.scss:.columns`, byte-identical to `GoalRow/_metrics.scss:.label` 40 lines away — the header and the rows of ONE table, so a change to the header's type scale silently leaves the mobile labels behind)
- [src/app/_components/DashboardScreen/components/CeilingCard/style.module.scss:.legend] three deviations from the imported design, only one of which the PR declared: the header/legend strips do NOT run edge-to-edge to the card border (task 03 asked for that and for a comment saying which way it was resolved — neither landed), the "compacta" density the owner picked was not applied (`MeterRow`'s `.row` still has `padding: var(--space-3) 0`), and the coloured dots beside each headline caption were dropped because `Headline` is shared with `StatCard` and takes a plain string caption (declared in the PR body), until the card is next revisited against the design (2026-07-30, found in PR #91 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/style.module.scss:11] ~25 lines of dense-numeric-table CSS (`.caption`, `.table`, the `th, td` block, `thead th`) are a near-verbatim copy of `src/app/leitor-ofx/_components/OfxScreen/components/ReportView/components/MonthTable/style.module.scss` — task 02 instructed the copy, so this is the intended outcome and not a slip, until one mixin in `src/styles/` holds the block and both stylesheets include it (2026-07-30, found in PR #92 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/style.module.scss:36] the five-declaration micro-label block already recorded at `IntervalCard/style.module.scss:26` and `CeilingCard/_badges.scss:.limit, .now` reached a new folder here, twice more — also `MonthTable/_stacked.scss:.table tbody td::before`; `.current` is a third, now in this same file, until one mixin holds the block and every site includes it (2026-07-30, found in PR #92 review; `_groups.scss` site folded into this file by PR #93)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/index.tsx:27] `scope="colgroup"` is inert on both group headers: it associates a header with its column GROUP, and the table declares no `<colgroup>`/`<col>` elements, so the attribute claims an association the markup does not make — the file's own comment already concedes the `aria-label` is what carries it, until `<colgroup />` plus two `<colgroup span={3} />` follow the caption, or the attribute goes (2026-07-30, found in PR #92 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/style.module.scss:4] this comment and `MonthTable/_stacked.scss:7` both explain the container query by saying the card sits inside `Overview`'s `lg` grid — `Board/index.tsx:18` renders it as a SIBLING of `<Overview>`; the real cause of the non-monotonic width is the app shell's aside at `md`, which takes `<main>` from 767px to 504px between viewports 767 and 768. The container query is still the right mechanism and only the stated reason misleads, until someone next edits either file (2026-07-30, found in PR #92 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/_stacked.scss:29] the `<thead>` is clipped rather than hidden so the column headers survive `display: block`, but each stacked cell then carries BOTH an associated column header and its own `::before` label, so a table-navigating screen reader hears the column name twice on every cell — belt and braces on purpose, until the doubled announcement is measured as worse than the association it protects (2026-07-30, found in PR #92 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/index.tsx:"Teto do mês" cell] the presentational minus is unconditional, so a zero ceiling renders the literal `−R$ 0,00` — on a 30-month range 22 of 30 rows show it, which is the tail of the geometric decay already recorded at `pace.helper.ts:savingPace` surfacing as a signed zero, until a zero spend drops the sign (2026-07-30, found in PR #92 review)
- [src/app/_components/DashboardScreen/components/CeilingCard/components/MonthTable/_stacked.scss:19] the collapse threshold still has less headroom than the card's width guarantees, re-measured at four columns: min-content is 442px with today's figures, 481px at `R$ 187.440,00` and 528px at `R$ 1.499.520,00`, against a 489px threshold — at a 900px viewport the card is 504px, so it stays tabular and a seven-figure balance overflows its wrapper by 24px (measured by substituting the figures into the live table), until seven-figure balances become realistic or the threshold tracks the content (2026-07-30, found in PR #92 review; re-measured in PR #93, which cut the table from seven columns to four)
- [src/app/api/dashboard/ceiling.helper.ts:buildCeiling loop] raising the cap LOWERS most later rows of the month table — on the owner's `dev.db` (familia, 18 months) 14 of 18 fall going from 50 to 75, e.g. Nov/26 R$ 3.044,11 -> R$ 2.296,44 — because the accumulator gives the early months more and leaves the later ones a smaller balance to take their share of; the card shows no sign of the redistribution, so a reader who clicks 75% expecting more room watches most rows shrink. The hint and the story's criterion now say this instead of the reverse, and `expectCapOrder` pins month 0 alone, so nothing states a falsehood — until the owner decides whether the reversal should be surfaced on the card (a note when a row falls), removed (spread the cap evenly over the remaining months rather than compounding), or left as is (2026-07-30, found in PR #93 review)
- [src/app/api/dashboard/ceiling.types.ts:12] the comment promises `ceilingBalance` and `ceilingLeft` "can never go negative" as an unconditional property of the suffix minimum, but that reasoning only holds while `cap <= 100`, and `buildCeiling(points, currentMonth, cap: number)` takes a plain number — the only thing enforcing the bound is the `z.enum(CEILING_CAPS)` in `route.ts:19`, one caller away; `MonthTable/style.module.scss:.left` leans on the same promise by having no negative state, until the comment names the condition or `buildCeiling` takes `CeilingCap` so the type carries the bound (2026-07-30, found in PR #93 review)
- [src/lib/ceiling-caps.ts:4] the comment says "the arithmetic in `buildCeiling` converts once", but the string-to-number conversion is `.transform(Number)` in `src/app/api/dashboard/route.ts:21`; `buildCeiling` receives a number it never converts, so the one file that exists to keep both ends of the wire agreeing points at the wrong end, until someone next edits either file (2026-07-30, found in PR #93 review)
- [src/app/_components/DashboardScreen/hook.ts:refreshing] when a cap refetch FAILS the selector keeps showing the newly picked cap while the board still shows the previous cap's figures — the red error notice above it is the only thing saying so, which was accepted over blanking the board, until the selector reverts to the cap the visible figures belong to on error (2026-07-30, named in PR #93's Known gaps)
- [src/app/_components/DashboardScreen/components/MonthlyBarChart/index.tsx:19] the `PROJETADO` `ChartTag` rides `ChartFrame`'s `background` slot, which `ChartFrame/hook.ts` documents as drawn UNDER the gridlines — right for the wash beside it, wrong for an opaque label, and a tall bar in the first projected months can paint over it; `BalanceLineChart` passes the same component as a CHILD, so one component is layered two ways in one story, until the tag moves to the children list and both charts agree (2026-07-31, found in PR #94 review)
- [src/app/_components/DashboardScreen/components/StatCard/style.module.scss:.rows] the 2x2 grid plus `.value` at `--text-base` leaves each cell ~95px at 1024px, where `.kpis` is three columns — measured, every figure is 36px tall (two lines) there against 18px at 1440px, so `R$ 105.918,21` breaks after `R$`, which `GoalRow/style.module.scss` already refuses in a comment ("a figure that wrapped mid-number would read as two"); nothing overflows so no spec catches it, until `.value` drops a step at that tier or `.rows` collapses to one column below `xl` (2026-07-31, found in PR #94 review)
- [src/app/_components/DashboardScreen/components/Headline/hook.ts:4] three comments the dashboard-v2 story turned false and did not follow: this one still says CeilingCard hangs its weekly and daily splits off the Headline (they moved into `.rates` in the summary pane), `src/components/Tooltip/style.module.scss:44` still says the pinned strip applies "below `md`" while the rule 24 lines down now reads `xl`, and `src/components/EntryRow/style.module.scss:12` points at `HeroCard`, renamed to `HeroBand` — until someone next edits each file (2026-07-31, found in PR #94 review)
- [src/app/_components/DashboardScreen/components/GoalRow/index.tsx:5] this `GoalRow` collides by name with `src/app/configuracoes/_components/SettingsScreen/components/GoalsSection/components/GoalRow/` — both export `GoalRow`, `useGoalRow` and `GoalRowProps` and are unrelated (the settings one is an edit/delete row on `RowGrid`); nothing breaks while each is imported by folder-relative path from its single parent, but ARCHITECTURE.md's promotion rule moves a component to a shared `components/` folder on its second consumer and two same-named components can never both do that, until one is renamed for what it is (2026-07-31, found in PR #95 review)
- [src/app/_components/DashboardScreen/components/SavingsSection/style.module.scss:.card] `--goal-cols` opens with a raw `minmax(180px, 1.2fr)` and no comment saying the 180 came from `Dashboard v2.dc.html:31` or what it costs — measured at a 1024px viewport the floor binds, leaving the three metric tracks 133.3px each instead of the 138.1px an unfloored `1.2fr` would give them, which is the difference between a completion date on one line and on two; every other raw pixel here names its reason (`CeilingCard/_split.scss:20`, `CoverageBar/style.module.scss:9`), until the line carries the same (2026-07-31, found in PR #95 review)
- [src/app/_components/DashboardScreen/components/GoalRow/style.module.scss:.row] only the TRACKS were shared: `--goal-cols` makes the header and the rows agree on column widths, but a column's position also depends on the container's `gap` and inline padding, and `.row` here and `.columns` in `SavingsSection/style.module.scss` declare both independently in two folders — they agree today, and changing one for a denser table shifts every figure out from under its header with no compile error, no lint error and no spec (goals.spec.ts reads text and measures no box), until the gap and the inset ride the same custom-property mechanism the tracks do (2026-07-31, found in PR #95 review)
- [src/app/_components/DashboardScreen/components/SavingsSection/style.module.scss:.columns] the table is cramped between 1024px and about 1280px: measured card width and metric track are 1096/237px at 1440, 936/199px at 1280 and 680/133px at 1024, and a value like `~10 meses · Abr/2027` fits one line at 199px and wraps to two at 133px — the columns still align exactly and nothing overflows, but `lg` was picked specifically to avoid that wrap and does not achieve it; moving to `xl` is FIVE edits, not one (`.card` and `.columns` here, `.metrics`, `.metric` and `.label` in `GoalRow/_metrics.scss`), because `t.bp` takes a breakpoint name rather than a length, until the owner picks `xl` and all five move together (2026-07-31, named in PR #95's Known gaps)
- [.squad/stories/2026-07-31-simulated-forecasts/story.md:15] the acceptance criterion says the dashboard selector sits "above the hero band" and the box is ticked, but `DashboardScreen/index.tsx` renders it BELOW — `HeroBand` is full-bleed and cancels `<main>`'s padding with a negative margin on all four sides, so anything above it is overlapped by 16px (40px from `md`), measured; the PR body carries the reason and the story file does not, so the merged criterion and the code disagree, until the criterion is reworded or the band stops carrying a negative top margin (2026-08-01, found in PR #96 review)
- [src/app/previsoes/_components/ForecastsScreen/components/ForecastForm/style.module.scss:13] `.simulated` repeats nine of the ten declarations in `.lock` (`IntervalCard/style.module.scss:41`) — the same commit promoted the checkbox BOX to `src/styles/_checkbox.scss` because it gained a second consumer, and the LABEL it requires gained one at the same moment and was copied instead; both live in the same modal, so changing one silently makes the pair inconsistent, and this is also a new site of the five-declaration micro-label block already tracked at `IntervalCard/style.module.scss:26`, until `_checkbox.scss` gains a `@mixin label` both include (2026-08-01, found in PR #96 review)
- [src/app/_components/DashboardScreen/simulation.hook.ts:20] a stored view of `all` costs two full dashboard computations per page load: `localStorage` is read in a `useEffect` that runs AFTER the fetch effect has already fired against the default, so `/` requests `simulation=real` then immediately `simulation=all` — no correctness bug (the `current` flag discards the first response, verified: no flash, no out-of-order win) but `getDashboard` lists every movement, forecast and goal each time, until a load with a stored `all` fires exactly one request carrying it (2026-08-01, found in PR #96 review)
- [src/app/api/period/service.ts:8] `/api/period` has no simulation filter and `EntryScreen/hook.ts` feeds it straight into every row's `CoverageBar`, so one far-future what-if rescales the bar of every REAL forecast on /previsoes — measured, posting a simulated forecast for 203012 moved the period from `{start:202601,end:202712}` to `{end:203012}` while `/api/dashboard?simulation=real` correctly stayed at 24 months; the PR body names the endpoint as a known gap but describes it as "the coverage bar covers simulations too" and never states this effect on real rows, which is the pollution the story exists to prevent, until /previsoes draws its bars against a period that excludes simulations or the owner declines it knowing this (2026-08-01, found in PR #96 review)
- [src/app/_components/DashboardScreen/components/SimulationSelect/index.tsx:20] the two `<option value>` strings are hand-written literals and `hook.ts:8` types `onChange` as `(next: string) => void`, so `src/lib/simulation.ts`'s promise that both ends read from it holds only for the default while the vocabulary is duplicated — write `value="todos"` and nothing fails (TypeScript accepts a string, `isView` rewrites it to `real`, and the control visibly moves while changing nothing); the neighbouring `CapSelector/hook.ts:12` builds its options from `CEILING_CAPS` and types its callback with the union, until this one does the same (2026-08-01, found in PR #96 review)
- [src/app/previsoes/_components/ForecastsScreen/components/ForecastForm/index.tsx:49] the hint "O dashboard só soma simulações quando você pedir." is copy task 02 never asked for and the PR body never lists, so nobody reviewed its wording, and it is a bare `<p>` with no `aria-describedby` from the checkbox — a screen-reader user hears "Simulação, caixa de seleção" and never the sentence that explains what ticking it does, making the one line that sells the feature decoration for them; `TextField`/`SelectField` have no description slot so this is a new one-off, until the `<p>` carries an id the input points at or the line goes (2026-08-01, found in PR #96 review)
- [src/app/_components/DashboardScreen/simulation.hook.ts:35] `choose` writes to `localStorage` and moves the selector before the refetch it triggers is known to have succeeded, so a failed fetch leaves "Incluir simulações" reading over figures that exclude them AND persists that choice — same shape as the `cap` entry above, and worse here because the disagreement is qualitative rather than a percentage, so the reader cannot tell from the numbers which view they are looking at; the two should move together, until the selector reverts to the view the visible figures belong to on error (2026-08-01, found in PR #96 review)
- [src/app/_components/DashboardScreen/components/MonthlyBarChart/components/BarMark/index.tsx:40] every mark is `tabIndex={0}`, which is what finally made entradas/saídas per month reachable without a mouse — but on the owner's 36-month range that is 72 tab stops on the bar chart and 36 more on the line chart, all before the ceiling table, until a roving tabindex (one focusable mark, arrow keys moving focus within the plot) replaces the per-mark stop (2026-08-13)
- [src/app/_components/DashboardScreen/components/GoalRow/hook.ts:METRICS] the three strategy hues are separated by HUE only: measured, the category ramp is luminance-flat, so its best-separated PAIR is 1.32:1 and these three are 1.12–1.28:1 — nobody reading in greyscale can tell the bars apart, and no choice from this ramp would change that. Accepted because the label above each bar and the landing date beside it carry the distinction and `_bar.scss` marks the bars aria-hidden, until the ramp gains lightness steps or the bars are dropped for being decoration (2026-08-13)
